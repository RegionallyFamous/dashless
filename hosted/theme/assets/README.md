# Selected working identity

Canonical selection: `brand/README.md` — **The Rip + Hot Type**, chosen in the “Evaluate Dashless name” task.

- `rip.svg`: exact selected vector sketch from `brand/post-punk-01/rip.svg`.
- `favicon.svg`: the same selected mark, replacing the previous typeset d. A dedicated optically tuned 16px master is still pending.
- `hot-type-v3.png`: unchanged selected concept sheet from `brand/logo-study-03/hot-type-v3.png`. The `.dl-hot-type` CSS viewport matches the selected brand comparison page and displays its primary wordmark, without redrawing or substituting its lettering.

The earlier Georgia-outline approximation has been removed. Hot Type remains raster concept art, not a completed vector master. Keep the selected contours when producing final vector lettering. No physical print proof is claimed.

Paper #F7F2E8, Ink #24212B, Acid #DFFF00 remain primary. Use heavy sans campaign headlines, monospace labels, readable body copy, and the approved descriptive voice. Branding belongs to the Dashless service, not every customer's publication.
